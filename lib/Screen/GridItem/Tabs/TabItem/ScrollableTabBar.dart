import 'package:flutter/material.dart';

class ScrollableTabBar extends StatelessWidget {
  const ScrollableTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
